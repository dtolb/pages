# Pages

HTML sites for sharing.

## Docs that moved out

`call-quality-escalation.html` is the published rendering of a design spec that
now lives with the code it produced, in the `aci-quality-poc` repo at
`docs/superpowers/specs/2026-07-30-call-quality-escalation-design.md` — along
with that POC's own design and both implementation plans.

`docs/superpowers/` here keeps only specs whose subject is not a separate repo.
